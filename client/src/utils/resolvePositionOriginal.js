/**
 * Original positioning - no overlap prevention
 * Just returns the position as-is from the positioning algorithm
 * @param {THREE.Vector3} desiredPos - The desired position from the positioning algorithm
 * @returns {THREE.Vector3} - The same position unchanged
 */
export const resolvePositionOriginal = (desiredPos) => {
  return desiredPos
}
